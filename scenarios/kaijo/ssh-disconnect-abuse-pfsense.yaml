type: leaky
name: kaijo/ssh-disconnect-abuse-pfsense
description: Detect SSH disconnect abuse on pfSense
filter: |
  evt.Meta.service == "ssh" && evt.Meta.log_type == "ssh_disconnect"
leakspeed: 10s
capacity: 5
groupby: evt.Meta.source_ip
blackhole: 2m
labels:
  service: ssh
  type: abuse
