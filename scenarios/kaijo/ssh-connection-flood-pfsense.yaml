type: leaky
name: kaijo/ssh-connection-flood-pfsense
description: Detect SSH connection floods on pfSense
filter: |
  evt.Meta.service == "ssh" && evt.Meta.log_type == "ssh_closed"
leakspeed: 5s
capacity: 10
groupby: evt.Meta.source_ip
blackhole: 1m
labels:
  service: ssh
  type: scan
